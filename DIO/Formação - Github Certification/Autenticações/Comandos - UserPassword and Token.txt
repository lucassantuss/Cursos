-> Autenticação via Usuario e Senha

git config --global user.name "lucas"

git config --global user.email "lucassantuss@outlook.com"

git config --list

git clone [URL do repositório]

git add .

git status

git commit -m "Atualizando meu arquivo README"

git push origin main

**logar na conta através do redirect feito

-> Autenticação via token

**criar token

mesmo processo de cima
git add / commit / push

**loga com o token gerado anteriormente no inicio